# SplitHive Backend

The full project README lives at the repository root:

```text
../README.md
```

Backend quick start:

```bash
cd backend
bundle install
bin/rails db:create
bin/rails db:migrate
bin/rails db:seed
bin/rails server -p 3000
```

This app targets Ruby `3.3.0` as defined in `.ruby-version`.
